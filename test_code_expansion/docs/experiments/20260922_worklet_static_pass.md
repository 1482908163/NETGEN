# 静态任务验证通过，恢复完整对照

数据：strong_scaling_results/mesh_algorithms_20260922-225844。
来源：各规模 route_a_static/pN/analysis/summary.csv、预热 quality_summary.json 和 pN/ROUTE_SUMMARY.txt。

| 指标 | 128 进程 | 256 进程 |
|---|---:|---:|
| 正式运行数（两种计时各三次） | 6 | 6 |
| 正常计时核心中位数/秒 | 6.557056 | 4.088467 |
| 预热审计四面体数 | 56944896 | 61623616 |
| 结构硬错误 | 0 | 0 |
| 正常计时计算不均衡比 | 1.844240 | 1.550628 |
| 任务完成等待最大值的中位数/秒 | 2.529621 | 1.181447 |
| 结果回传最大值的中位数/秒 | 0.013729 | 0.008702 |
| 最慢计算进程（三次均相同） | 1 | 1 |

已证明：上一轮 NG_OK 被诊断计数误判为失败的问题不再阻断本批运行；静态结果通过现有完整结构审计。
审计不等价于 CAD 几何和全局相交的完备证明。本批只有静态路线，不能据此证明动态收益或质量相对参考路径不退化。
不同进程数的生成单元数不同，不能直接把上述时间比称为固定工作量强扩展加速比。

静态派发规则将逻辑进程 0 的任务交给工作进程 1，因此进程 1 同时承担两份分区任务。
这与最慢进程观测一致，但单凭汇总数据不能把全部等待归因于此。
现有动态方案允许空闲工作进程领取剩余任务，下一批用于检验收益与回传代价。
暂不改变静态规则或任务切分，以便保持已验证的对照条件。

## 下一批

恢复默认 reference、a_static、a_dynamic、b_remaining、b_critical、c_deferred 六路线。
保持 128/256 进程、任务因子、线程布局、细化参数、预热和重复次数，沿用 run_experiments.sh 统一入口。
参考与候选同批运行，避免跨批次机器状态差异干扰结论。失败仍继续后续路线。

分析新增参考到三种动态方案的端到端直接对照。
这三组允许内部网格切分变化，但必须通过现有质量比较；动态策略之间继续要求所有权和任务网格指纹相同。
只有胜过静态任务路径并不足以说明胜过原算法。若质量门槛未过，即使时间缩短也不认定为有效加速。

本地验证：分析回归（含新增直接对照）、运行入口脚本回归、Shell 语法检查。
本次仅修改实验默认配置、分析及说明，不修改网格生成内核，不宣称新增集群实测结果。
